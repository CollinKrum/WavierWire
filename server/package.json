{
  "name": "fantasy-helper-server",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prebuild": "apt-get update && apt-get install -y python3 python3-pip && pip3 install espn-api || echo 'Python install failed - will continue anyway'",
    "build": "npm install",
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^8.1.0",
    "node-fetch": "^3.3.2",
    "pg": "^8.12.0"
  }
}
