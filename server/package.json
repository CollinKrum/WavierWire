{
  "name": "fantasy-helper-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "scripts": {
    "prebuild": "apt-get update && apt-get install -y python3 python3-pip && pip3 install espn-api || echo 'Python install failed'",
    "build": "npm install",
    "start": "node index.js"
  }
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.19.2",
    "node-fetch": "^3.3.2",
    "pg": "^8.11.0"
  }
}
